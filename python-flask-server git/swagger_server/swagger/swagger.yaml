---
swagger: "2.0"
info:
  description: "API for the parser microservice"
  version: "1.0.0"
  title: "Swagger Parser Microservice"
host: "localhost:8081"
basePath: "/api"
schemes:
  - "http"
paths:
  /admin:
    post:
      description: "Returns the information about the Station Admin by ID."
      operationId: "get_admin_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of Station Admin "
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "200":
          description: "OK. Information about Admin."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid ID provided."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No matching Station Admin found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/all:
    post:
      description: "Returns the IDs of all trains"
      operationId: "get_all_trains"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters: []
      responses:
        "200":
          description: "OK. IDs of all trains"
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No trains found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/all:
    post:
      description: "Returns IDs of all Stations."
      operationId: "get_all_stations"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters: []
      responses:
        "202":
          description: "OK. IDs of all stations."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No stations found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/owner:
    post:
      description: "Returns the information about the Station Owner by Station ID."
      operationId: "get_station_owner_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Name and email of Station Owner."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid Station ID provided."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No Station Owner for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/responsible:
    post:
      description: "Return the information about the Station Responsible by StationID."
      operationId: "get_station_resp_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Name and email of Station Responsible."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid Station ID provided."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No Station Responsible for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/compenv:
    post:
      description:
        "Return the information about the computational environment of\
        \ a Station by Station ID."
      operationId: "get_comp_env_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Computational environment information."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid Station ID provided."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No computational environment for given ID found"
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /user:
    post:
      description: "Return name and email of a user by user ID"
      operationId: "get_user_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of user"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. User information."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid user ID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No user for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/description:
    post:
      description: "Return the train's description given its ID"
      operationId: "get_train_description_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train description."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No description for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/info:
    post:
      description: "Return the train's information given its ID"
      operationId: "get_train_info_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train information."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No information for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/performance/CPU:
    post:
      description: "Return the train's CPU usage given its ID"
      operationId: "get_train_cpu_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train CPU usage."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No CPU usage for given ID found."
          schema:
            $ref: "#/definitions/SBFResImg"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/performance/memory:
    post:
      description: "Return the train's memory usage given its ID"
      operationId: "get_train_memory_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train memory usage."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No memory usage for given ID found."
          schema:
            $ref: "#/definitions/SBFResImg"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/performance:
    post:
      description: "Return the train's overall performance given its ID"
      operationId: "get_train_performance_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train performance."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No performance for given ID found."
          schema:
            $ref: "#/definitions/SBFResImg"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/train:
    post:
      description: "Return the trains at a station given the station's ID"
      operationId: "get_trains_at_station_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Trains at the station."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No trains for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/station:
    post:
      description: "Return the station a train is at given its ID"
      operationId: "get_cur_train_station_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Current train station."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No station for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/route/full:
    post:
      description: "Return the train's full route given its ID"
      operationId: "get_train_route_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train route."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No route for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/route/past:
    post:
      description: "Return the train's past route given its ID"
      operationId: "get_train_past_route_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train's past route."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No past route for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/route/upcomming:
    post:
      description:
        "Return the train's planned route (stations not yet visited) including\
        \ current station given its ID"
      operationId: "get_train_future_route_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train's future route."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No planned route for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/train/upcomming:
    post:
      description: "Return the trains that will visited a station next given its ID"
      operationId: "get_upcomming_trains_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Trains that will visit station next."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No incomming trains for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/creator:
    post:
      description: "Return the train's creator given its ID"
      operationId: "get_train_creator_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train creator."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No creator for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/publisher:
    post:
      description: "Return the train's publisher given its ID"
      operationId: "get_train_publisher_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train publisher."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No publisher for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/title:
    post:
      description: "Return the train's title given its ID"
      operationId: "get_train_title_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train title."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No title for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/version:
    post:
      description: "Return the train's version given its ID"
      operationId: "get_train_version_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train version."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No version for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/cert:
    post:
      description: "Return the train's certificates given its ID"
      operationId: "get_train_certificate_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train certificate."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No certificate for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/model:
    post:
      description: "Return the train's model given its ID"
      operationId: "get_train_model_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train model."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No model for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/running:
    post:
      description: "Return all running trains."
      operationId: "get_running_trains"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters: []
      responses:
        "202":
          description: "OK. All running trains."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No running trains found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/error:
    post:
      description: "Return errors train encountered given its ID."
      operationId: "get_train_errors_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train errors."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No error for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/error:
    post:
      description:
        "Return the errors a train encountered at a station given the stations\
        \ ID"
      operationId: "get_station_errors_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train errors."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No errors for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/rejected:
    post:
      description: "Return the a train's rejections given its ID"
      operationId: "get_train_rejections_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train's rejections."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No rejections for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/train/rejected:
    post:
      description:
        "Return the rejections a train encountered at a station given the\
        \ stations ID"
      operationId: "get_station_rejections_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station rejections."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No rejections for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/log:
    post:
      description: "Return the a train's log given its ID"
      operationId: "get_train_log_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train logs."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No logs for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/log:
    post:
      description: "Return the a train's logs at a station given its ID"
      operationId: "get_station_log_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train logs at station."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No logs for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/expecteddata:
    post:
      description: "Return the a train's expected data given its ID"
      operationId: "get_train_data_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train's expected data."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No expected data for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/started/running:
    post:
      description: "Return the a last started run given its ID"
      operationId: "get_train_started_running_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. last started run."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No started runs for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/train/started/running:
    post:
      description: "Return the the started runs on a station given its ID"
      operationId: "get_station_started_running_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Finished runs on station."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No started runs for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/finished/running:
    post:
      description: "Return the a train's last finished  given its ID"
      operationId: "get_train_finished_running_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. train's last finished ."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No finished runs for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/train/finished/running:
    post:
      description: "Return the the finished runs on a station given its ID"
      operationId: "get_station_finished_running_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Runs finished on station."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No finished runs for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /train/finished/transmission:
    post:
      description: "Return the a train's finished transmissons given its ID"
      operationId: "get_train_finished_transmission_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of train"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Train's finished transmissions."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid trainID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No finished transmissons for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/train/finished/transmission:
    post:
      description: "Return the the finished tranmissions on a station given its ID"
      operationId: "get_station_finished_transmission_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Transmissions finished on station."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No finished transmissions for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/cert:
    post:
      description: "Return a station's certificate given its ID"
      operationId: "get_station_cert_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's certificate."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No certificate for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/description:
    post:
      description: "Return station's description given its ID"
      operationId: "get_station_description_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's desciption."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No description for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/title:
    post:
      description: "Return station's title given its ID"
      operationId: "get_station_title_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's title."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No title for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/rights:
    post:
      description: "Return a station's rights given its ID"
      operationId: "get_station_rights_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's rights."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No rights for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/location:
    post:
      description: "Return a station's location given its ID"
      operationId: "get_station_location_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's location."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No location for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/dataset:
    post:
      description: "Return the dataset of a station given its ID"
      operationId: "get_station_dataset_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's dataset."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No dataset for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/info:
    post:
      description: "Return all info of a station given its ID"
      operationId: "get_station_info_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's information."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No information for given ID found."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /station/performance:
    post:
      description: "Return the perfomances on a station given its ID"
      operationId: "get_station_performance_by_id"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "json_input"
          description: "ID of station"
          required: true
          schema:
            $ref: "#/definitions/SBF"
      responses:
        "202":
          description: "OK. Station's performance."
          schema:
            $ref: "#/definitions/SBFRes"
        "400":
          description: "Invalid stationID."
          schema:
            $ref: "#/definitions/SBFRes"
        "404":
          description: "No performances for given ID found."
          schema:
            $ref: "#/definitions/SBFResImg"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /coffee:
    post:
      description: "Get coffee"
      operationId: "get_coffee"
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters: []
      responses:
        "418":
          description: "I'm a teapot."
          schema:
            $ref: "#/definitions/SBFRes"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
definitions:
  SBF:
    type: "object"
    required:
      - "botName"
      - "channel"
      - "email"
      - "entities"
      - "intent"
      - "time"
      - "user"
    properties:
      mgs:
        type: "string"
      botName:
        type: "string"
      channel:
        type: "string"
      intent:
        type: "string"
      entities:
        type: "object"
        properties: {}
      email:
        type: "string"
      user:
        type: "string"
      time:
        type: "string"
    example:
      entities: "entities"
      botName: "botName"
      mgs: "mgs"
      channel: "channel"
      time: "time"
      intent: "intent"
      user: "user"
      email: "email"
  SBFRes:
    type: "object"
    properties:
      text:
        type: "string"
      closeContext:
        type: "string"
    example:
      text: "text"
      closeContext: "closeContext"
  SBFResImg:
    type: "object"
    properties:
      fileBody:
        type: "string"
      fileName:
        type: "string"
      fileType:
        type: "string"
